
void capture () {
  TV.capture();
  //TV.capture(0,0,W,H-8);
  //tv.fill(INVERT);
  //TV.resume();
  /*if (!other_enabled) {
    TV.delay_frame(1);
    sprintf(s, "%5u^e ls", millis()/100.0);
    TV.print(0, 10, s);
    if (capture_enabled) {
      drawAxes();
    }
  } else {
    //graphscene();
    TV.delay_frame(2);
  }*/
  /*if (other_enabled && capture_enabled)
    draw_cube();*/
  //delay(1);
  /*if (other_enabled) {
    graphscene();
  }*/
}
